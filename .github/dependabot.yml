# Dependabot keeps the actions current. Actions are pinned by commit with
# the tag in a trailing comment; an update moves the commit and the
# comment together. One weekly pull request carries every action update.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
